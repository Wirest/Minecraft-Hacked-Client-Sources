package awfdd.ksksk.zabejhf.rgsd;


/**
 * An abstract listener class that implements {@link JAHfb}.
 * 
 * @author TCB
 *
 */
public abstract class FinalDaCappa implements JAHfb {
	/**
	 * Returns whether this listener should receive events.
	 * True by default.
	 * @return Boolean
	 */
	@Override
	public boolean isEnabled() {
		return true;
	}
}